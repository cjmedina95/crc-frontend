describe('API Reachable', () => {
  it('passes', () => {
    cy.request('GET', 'https://counterhttpfunc.azurewebsites.net/api/counter/get').then(
		(response) => {
			expect(response.body).to.have.property('id', 'VisitCount')
		}
	)
  })
  
  it('passes', () => {
	cy.request('https://counterhttpfunc.azurewebsites.net/api/counter/increment')  
  })
})