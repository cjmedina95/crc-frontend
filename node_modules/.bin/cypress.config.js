const { defineConfig } = require('cypress')

module.exports = defineConfig({
	// The rest of the Cypress config options go here...
	projectId: "34fi85",

	e2e: {
		baseUrl: 'https://counterhttpfunc.azurewebsites.net/api/counter/',
		supportFile: false
	},
});
